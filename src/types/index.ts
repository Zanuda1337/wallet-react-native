import {	Animated, RegisteredStyle,	ViewStyle} from "react-native";export type AnimatedStyle = Animated.Value | RegisteredStyle<ViewStyle> | Animated.AnimatedInterpolation<string | number> | Animated.WithAnimatedObject<ViewStyle>export type TMoneyFlow = { category: string; amount: number }export type TBalance = {itemId: number, name: string, balance: number}export type TDynamic = {  id: number;  date: Date;  expenses: TMoneyFlow[];  incomes: TMoneyFlow[];};