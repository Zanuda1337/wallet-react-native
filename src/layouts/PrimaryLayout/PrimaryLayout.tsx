import React from "react";import { View } from "react-native";import { Outlet, useLocation } from "react-router-native";import Header from "src/components/header/Header";import Navigation from "src/components/navigation/Navigation";import { routes } from "src/features/router/Router.consts";import {useIntl} from "react-intl";import {capitalize} from "src/utils";type TPrimaryLayoutProps = {};const PrimaryLayout: React.FC<TPrimaryLayoutProps> = ({}) => {  const location = useLocation()  const route = routes.find(route => route.path === location.pathname);  const intl = useIntl()  return (    <View style={{flex: 1}}>      <Header label={capitalize(intl.formatMessage({id:route?.label}), true)} />      <Outlet />      <Navigation />    </View>  );};export default PrimaryLayout;