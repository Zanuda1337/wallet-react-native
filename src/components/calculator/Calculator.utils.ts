const operationsRegEpx = /\+|-|\*|\//;export const calculate = (text: string): number | false => {  try {    return eval(trimExpression(text));  } catch (e) {    return false;  }};export const extractNumbers = (text) =>  text    .split(operationsRegEpx)    .filter((char) => !!char && !isNaN(+char))    .map((char) => +char);export const extractOperations = (text, regExp = operationsRegEpx) =>  text.split("").filter((char) => regExp.test(char));export const trimExpression = (text) => {	const numbers = extractNumbers(text)	const operations = extractOperations(text)	if (numbers.length === operations.length || text.at(-1) === '.')		return  text.slice(0, text.length - 1);	return text}