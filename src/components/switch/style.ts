import { createStyles } from "src/utils";export const switchStyles = createStyles((theme) => ({  track: {    width: 50,    height: 28,    display: "flex",    flexDirection: "row",    alignItems: "center",    padding: 2,    borderRadius: 14,    backgroundColor: theme.colors.foreground,  },	checkboxChecked: {    backgroundColor: theme.colors.checkboxChecked,	},  checkboxThumb: {    backgroundColor: theme.colors.background,  },}));