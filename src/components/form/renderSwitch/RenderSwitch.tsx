import React from "react";import { UseControllerReturn } from "react-hook-form/dist/types";import Switch from "src/components/switch/Switch";import { Text, View } from "react-native";import {FormattedMessage} from "react-intl";import {useTheme} from "src/hooks";interface IRenderCheckboxProps extends UseControllerReturn<any> {  label: string}const RenderSwitch: React.FC<IRenderCheckboxProps> = ({  label,  field: { value, onChange },}) => {  const theme = useTheme();  return (    <View style={theme.styles.item}>      <Text style={theme.styles.label}><FormattedMessage id={label} /></Text>      <View>        <Switch checked={value} onChange={() => onChange(!value)} />      </View>    </View>  );};export default RenderSwitch;