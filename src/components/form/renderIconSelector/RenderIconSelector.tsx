import React from "react";import IconSelector from "src/components/iconSelector/IconSelector";import { UseControllerReturn } from "react-hook-form/dist/types";interface IRenderIconSelectorProps extends UseControllerReturn<any> {  backgroundColor: string;  label: string;}const RenderIconSelector: React.FC<IRenderIconSelectorProps> = ({  field: { value, onChange },  label,  backgroundColor,}) => {  return (    <IconSelector      label={label}      value={value}      onChange={onChange}      backgroundColor={backgroundColor}    />  );};export default RenderIconSelector;