import React from 'react';import {Text, View} from "react-native";import {useStyles, useTheme} from "src/hooks";import {histogramStyles} from "src/components/histogram/style";type TBarProps = {	height: number;	a: number;	b: number;	label: string;	zoom: number;	displayHeight: number}const Bar: React.FC<TBarProps> = ({height, a, b, label, zoom, displayHeight,}) => {	const style = useStyles(histogramStyles);	const theme = useTheme();	return (		<View style={[style.itemContainer]}>			<View style={[style.barWrapper, { height }]}>				<View					style={[						style.bar,						{							height: (b / displayHeight) * height * zoom,							backgroundColor: theme.colors.error,						},					]}				/>				<View					style={[						style.bar,						{							height: (a / displayHeight) * height * zoom,							right: 10,							backgroundColor: theme.colors.blue,						},					]}				/>			</View>			<View style={style.labelWrapper}>				<Text style={style.label}>{label}</Text>			</View>		</View>	);};export default Bar;