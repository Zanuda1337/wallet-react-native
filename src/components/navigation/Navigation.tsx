import React from "react";import { View } from "react-native";import { style } from "./style";import { theme } from "src/assets/styles/theme";import { routes } from "features/router/Router.consts";import { Link } from "react-router-native";import SvgSelector from "src/components/svgSelector/SvgSelector";type TNavigationProps = {};const Navigation: React.FC<TNavigationProps> = ({}) => {  return (    <View style={[style.container, theme.styles.container]}>      <View style={style.widget}>        {routes.map((route) => (          <Link key={route.path} to={route.path} >            <SvgSelector id={route.icon} size={30} fill={theme.colors.pale} />          </Link>        ))}      </View>    </View>  );};export default Navigation;