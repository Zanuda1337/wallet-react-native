import React from "react";import { View } from "react-native";import { navigationStyle } from "./style";import { useStyles, useTheme } from "src/hooks";import NavLink from "src/components/navigation/navLink/NavLink";import {getRoutesList} from "features/router/Router.utils";type TNavigationProps = {};const Navigation: React.FC<TNavigationProps> = ({}) => {  const style = useStyles(navigationStyle);  const theme = useTheme();  return (    <View style={[style.container, theme.styles.container]}>      <View style={style.widget}>        {getRoutesList().filter(route => route.show).map((route) => (            <NavLink key={route.path} path={route.path} iconId={route.icon} />        ))}      </View>    </View>  );};export default Navigation;