import {createStyles} from "src/utils";export const navigationStyle = createStyles(theme => ({  container: {    position: "absolute",    bottom: 0,    left: 0,    width: "100%",  },  widget: {    display: "flex",    justifyContent: "space-between",	  backgroundColor: theme.colors.popUp,	  alignItems: "center",	  flexDirection: "row",		height: 77,	  paddingVertical: 25,	  paddingHorizontal: 28,	  borderRadius: 38,	  shadowColor: theme.colors.commonShadow,	  shadowOpacity: 0.5,	  shadowRadius: 15,	  elevation: 7,  },}));