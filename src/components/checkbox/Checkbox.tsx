import React from 'react';import IconButton from "src/components/iconButton/IconButton";import SvgSelector from "src/components/svgSelector/SvgSelector";import {useTheme} from "src/hooks";type TCheckboxProps = {	checked: boolean;	onChange: () => void;	indeterminate: boolean}const Checkbox: React.FC<TCheckboxProps> = ({checked, indeterminate, onChange}) => {	const getIcon = () => {		if(indeterminate) return 'checkboxIndeterminate'		if(checked) return 'checkboxChecked'		return 'checkboxUnchecked'	}	const getColor = () => {		if(indeterminate) return theme.colors.primaryLight		if(checked) return theme.colors.primaryLight		return theme.colors.foreground	}	const theme = useTheme();	return (		<IconButton onPress={onChange}  size={30} variant='ghost' icon={<SvgSelector id={getIcon()} fill={getColor()} size={20}/>}/>	);};export default Checkbox;