import React from "react";import Svg, { Circle } from "react-native-svg";import {clamp} from "src/utils";import {StyleProp, ViewStyle} from "react-native";interface ICircularProgressProps {  progress: number;  stroke: string;  size?: number;  strokeWidth?: number;  opacity?: number;  offset?: number;  styles?: { root: StyleProp<ViewStyle> }}const CircularProgress: React.FC<ICircularProgressProps> = ({  progress,  stroke,  opacity = 1,  size = 50,  offset = 0,  strokeWidth = 1.5,  styles}) => {  const radius = size * 0.25;  const diameter = radius * 2;  const strokeDasharray = diameter * Math.PI;  const strokeDashOffset = Math.PI * diameter * ((100 - clamp(progress, 0, 100)) / 100);  const rotate = offset/100*360  return (    <Svg      width={size}      height={size}      viewBox={`0 0 ${diameter + strokeWidth} ${diameter + strokeWidth}`}      style={[{transform: [{rotate: `${rotate-90}deg`}]}, styles?.root]}    >      <Circle        r={radius}        cx={radius + strokeWidth * 0.5}        cy={radius + strokeWidth * 0.5}        fill="transparent"        strokeWidth={strokeWidth}        strokeDasharray={strokeDasharray}        strokeDashoffset={strokeDashOffset}        stroke={stroke}        opacity={progress > 0 ? opacity : 0}      />    </Svg>  );};export default CircularProgress;