import React from "react";import moment from "moment";import { useIntl } from "react-intl";import { capitalize } from "src/utils";type TMomentProviderProps = {  children: JSX.Element;};const MomentProvider: React.FC<TMomentProviderProps> = ({ children }) => {  const intl = useIntl();  moment.updateLocale(intl.locale, {    months: intl      .formatMessage({ id: "months" })      .split("_")      .map((s) => capitalize(s)),    monthsShort: intl      .formatMessage({ id: "months_short" })      .split("_")      .map((s) => capitalize(s)),    weekdays: intl      .formatMessage({ id: "weekdays" })      .split("_")      .map((s) => capitalize(s)),    weekdaysMin: intl      .formatMessage({ id: "weekdays_min" })      .split("_")      .map((s) => capitalize(s)),    week: { dow: 1, doy: 4 },  });  return children;};export default MomentProvider;