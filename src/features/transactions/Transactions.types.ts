export type TItem = {	id: number;	name: string;	type: TItemCategories	icon: string;	cashFlow: number}export type TItemBase = Omit<TItem, 'id'>export type TItemCategories = 'income' | 'wallet' | 'expense'export type TTransaction = {	id: number;	date: Date;	amount: number;	fromItemId: number;	toItemId: number;	isRepeat: boolean;	note: string;}export type TTransactionBase = Omit<TTransaction, 'id'>export enum Categories {	income='income',	wallet='wallet',	expense='expense'}