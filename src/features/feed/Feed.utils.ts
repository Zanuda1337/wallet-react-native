import {daysPast, pureDate} from "src/utils";import moment from "moment";import {IntlShape} from "react-intl";export   const getTitle = (date: Date, intl: IntlShape): string => {	const daysFromNow = daysPast(pureDate(), date)	if(daysFromNow < 1) return intl.formatMessage({id: "today"});	const momentDate = moment(date)	const firstMonthDay = moment(pureDate()).startOf('month');	if(momentDate.isSameOrAfter(firstMonthDay) && momentDate.month() === firstMonthDay.month()) return momentDate.format("dddd, Do MMM");	const firstYearDay = moment(pureDate()).startOf('year');	if(momentDate.isSameOrAfter(firstYearDay) && momentDate.year() === firstYearDay.year()) return momentDate.format("Do MMM");	return momentDate.format('DD.MM.YYYY');}