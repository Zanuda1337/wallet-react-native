import React from "react";import { View, Text } from "react-native";import {useTheme} from "src/hooks";import Charts from "features/analytics/charts/Charts";import {useAppSelector} from "src/store/hooks";type TAnalyticsProps = {};const Analytics: React.FC<TAnalyticsProps> = ({}) => {	const theme = useTheme();	const items = useAppSelector(state => state.transactionsReducer.items);	const transactions = useAppSelector(state => state.transactionsReducer.transactions);  return (    <View style={theme.styles.container}>      <Text style={theme.styles.title}>Activity</Text>	    <Charts transactions={transactions} items={items}/>    </View>  );};export default Analytics;