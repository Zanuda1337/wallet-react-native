import React from "react";import { View } from "react-native";import Histogram from "src/components/histogram/Histogram";import { TItem, TTransaction } from "features/transactions/Transactions.types";import { getDynamics } from "src/utils";import moment from "moment";type TChartsProps = {  transactions: TTransaction[];  items: TItem[];};const Charts: React.FC<TChartsProps> = ({ transactions, items }) => {  const dynamics = getDynamics(transactions, items, "months");  return (    <Histogram      height={220}      data={dynamics.map((value) => ({        a: value.incomes,        b: value.expenses,	      label: moment(value.date).format('MMM')      }))}    />  );};export default Charts;