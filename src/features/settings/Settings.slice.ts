import { createSlice, PayloadAction } from "@reduxjs/toolkit";import { getLocales } from "expo-localization";interface SettingsState {  theme: "system" | "light" | "dark";  locale: string;  currency: string;  initialized: boolean;}const initialState: SettingsState = {  theme: "light",  locale: "en",  currency: "USD",  initialized: false,};const locales = ["ru", "en", "uk", "be"];const currencies = ["USD", "RUB"];const settingsSlice = createSlice({  name: "settingsReducer",  initialState,  reducers: {    setSettings: (      state,      action: PayloadAction<{        theme: "system" | "light" | "dark";        locale: string;        currency: string;      }>    ) => {      state.theme = action.payload.theme;      state.locale = action.payload.locale;      state.currency = action.payload.currency;    },    init: (state) => {      const systemLocale = getLocales()[0];      const defaultLocale = locales.includes(systemLocale?.languageCode)        ? systemLocale?.languageCode        : "en";      const defaultTheme = "system";      const defaultCurrency = currencies.includes(systemLocale?.currencyCode)        ? systemLocale?.currencyCode        : "USD";      state.theme = defaultTheme;      state.locale = defaultLocale;      state.currency = defaultCurrency;      state.initialized = true;    },  },});export const { actions: settingsActions, reducer: settingsReducer } =  settingsSlice;